CREATE TABLE phones
(
    id        BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    number    TEXT,
    client_id BIGINT
);

-- ALTER TABLE phones
--     ADD CONSTRAINT FK_PHONES_ON_CLIENT FOREIGN KEY (client_id) REFERENCES client (id);